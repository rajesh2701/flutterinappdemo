enum PurchaseType {
  subscriptionPurchase,
  nonSubscriptionPurchase,
}

enum Store {
  googlePlay,
  appStore,
}

enum Status {
  pending,
  completed,
  active,
  expired,
}
enum ProductStatus {
  purchasable,
  purchased,
  pending,
}

enum StoreState {
  loading,
  available,
  notAvailable,
}
